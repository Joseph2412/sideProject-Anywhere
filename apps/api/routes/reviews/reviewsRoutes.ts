import { FastifyInstance } from 'fastify';

//import { reviewsRoutes } from './reviews';

export async function reviewsRoutes(fastify: FastifyInstance) {
  reviewsRoutes(fastify); //Rotta per PRENDERE nel sistema le recensioni
}
