# DATABASE CONFIGURATION
# URL di connessione al database PostgreSQL
# Formato: postgresql://username:password@host:port/database_name?schema=schema_name
DATABASE_URL="postgresql://username:password@localhost:5432/database_name?schema=public"

# JWT CONFIGURATION
# Chiave segreta per la generazione e verifica dei token JWT
# Generare una stringa casuale sicura di almeno 32 caratteri
JWT_SECRET="your-jwt-secret-key-here"

# API AND APP HOSTS
# URL dell'host API (backend)
API_HOST=http://localhost:3001

# URL dell'host dell'applicazione (frontend)
APP_HOST=http://localhost:3000

# URL pubblico dell'API accessibile dal frontend
NEXT_PUBLIC_API_HOST=http://localhost:3001

# URL pubblico dell'applicazione
NEXT_PUBLIC_APP_HOST=http://localhost:3000

# AWS S3 CONFIGURATION
# Endpoint del servizio S3 AWS
S3_ENDPOINT=https://s3.eu-south-1.amazonaws.com/

# Nome del bucket S3 per il salvataggio dei file
S3_REPORTS_BUCKET=your-bucket-name

# Regione AWS del bucket S3
S3_REGION=eu-south-1

# Chiave di accesso AWS per il servizio S3
S3_ACCESS_KEY=your-access-key

# Chiave segreta AWS per il servizio S3
S3_SECRET_KEY=your-secret-key

# GOOGLE OAUTH CONFIGURATION
# Client ID dell'applicazione Google OAuth
GOOGLE_OAUTH_CLIENT_ID=your-google-client-id

# Client Secret dell'applicazione Google OAuth
GOOGLE_OAUTH_CLIENT_SECRET=your-google-client-secret
